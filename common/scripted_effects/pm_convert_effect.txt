pm_convert_custom = {
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_food_industry
     }
     has_active_production_method = pm_bakery
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_food_industry
   production_method = pm_bakery
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_food_industry
     }
     has_active_production_method = pm_sweeteners
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_food_industry
   production_method = pm_sweeteners
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_food_industry
     }
     has_active_production_method = pm_baking_powder
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_food_industry
   production_method = pm_baking_powder
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_food_industry
     }
     has_active_production_method = pm_disabled_canning
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_food_industry
   production_method = pm_disabled_canning
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_food_industry
     }
     has_active_production_method = pm_cannery
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_food_industry
   production_method = pm_cannery
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_food_industry
     }
     has_active_production_method = pm_vacuum_canning
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_food_industry
   production_method = pm_vacuum_canning
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_food_industry
     }
     has_active_production_method = pm_disabled_distillery
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_food_industry
   production_method = pm_disabled_distillery
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_food_industry
     }
     has_active_production_method = pm_pot_stills
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_food_industry
   production_method = pm_pot_stills
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_food_industry
     }
     has_active_production_method = pm_patent_stills
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_food_industry
   production_method = pm_patent_stills
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_food_industry
     }
     has_active_production_method = pm_manual_dough_processing
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_food_industry
   production_method = pm_manual_dough_processing
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_food_industry
     }
     has_active_production_method = pm_automated_bakery
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_food_industry
   production_method = pm_automated_bakery
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_food_industry
     }
     has_active_production_method = pm_merchant_guilds_building_food_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_food_industry
   production_method = pm_merchant_guilds_building_food_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_food_industry
     }
     has_active_production_method = pm_privately_owned_building_food_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_food_industry
   production_method = pm_privately_owned_building_food_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_food_industry
     }
     has_active_production_method = pm_publicly_traded_building_food_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_food_industry
   production_method = pm_publicly_traded_building_food_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_food_industry
     }
     has_active_production_method = pm_government_run_building_food_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_food_industry
   production_method = pm_government_run_building_food_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_food_industry
     }
     has_active_production_method = pm_worker_cooperative_building_food_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_food_industry
   production_method = pm_worker_cooperative_building_food_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_textile_mills
     }
     has_active_production_method = pm_handsewn_clothes
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_textile_mills
   production_method = pm_handsewn_clothes
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_textile_mills
     }
     has_active_production_method = pm_dye_workshops
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_textile_mills
   production_method = pm_dye_workshops
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_textile_mills
     }
     has_active_production_method = pm_sewing_machines
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_textile_mills
   production_method = pm_sewing_machines
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_textile_mills
     }
     has_active_production_method = pm_electric_sewing_machines
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_textile_mills
   production_method = pm_electric_sewing_machines
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_textile_mills
     }
     has_active_production_method = pm_no_luxury_clothes
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_textile_mills
   production_method = pm_no_luxury_clothes
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_textile_mills
     }
     has_active_production_method = pm_craftsman_sewing
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_textile_mills
   production_method = pm_craftsman_sewing
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_textile_mills
     }
     has_active_production_method = pm_elastics
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_textile_mills
   production_method = pm_elastics
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_textile_mills
     }
     has_active_production_method = pm_traditional_looms
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_textile_mills
   production_method = pm_traditional_looms
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_textile_mills
     }
     has_active_production_method = pm_mechanized_looms
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_textile_mills
   production_method = pm_mechanized_looms
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_textile_mills
     }
     has_active_production_method = pm_automatic_power_looms
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_textile_mills
   production_method = pm_automatic_power_looms
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_textile_mills
     }
     has_active_production_method = pm_merchant_guilds_building_textile_mills
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_textile_mills
   production_method = pm_merchant_guilds_building_textile_mills
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_textile_mills
     }
     has_active_production_method = pm_privately_owned_building_textile_mills
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_textile_mills
   production_method = pm_privately_owned_building_textile_mills
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_textile_mills
     }
     has_active_production_method = pm_publicly_traded_building_textile_mills
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_textile_mills
   production_method = pm_publicly_traded_building_textile_mills
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_textile_mills
     }
     has_active_production_method = pm_government_run_building_textile_mills
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_textile_mills
   production_method = pm_government_run_building_textile_mills
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_textile_mills
     }
     has_active_production_method = pm_worker_cooperative_building_textile_mills
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_textile_mills
   production_method = pm_worker_cooperative_building_textile_mills
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_furniture_manufacturies
     }
     has_active_production_method = pm_handcrafted_furniture
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_furniture_manufacturies
   production_method = pm_handcrafted_furniture
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_furniture_manufacturies
     }
     has_active_production_method = pm_lathe
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_furniture_manufacturies
   production_method = pm_lathe
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_furniture_manufacturies
     }
     has_active_production_method = pm_mechanized_workshops
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_furniture_manufacturies
   production_method = pm_mechanized_workshops
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_furniture_manufacturies
     }
     has_active_production_method = pm_no_luxuries
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_furniture_manufacturies
   production_method = pm_no_luxuries
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_furniture_manufacturies
     }
     has_active_production_method = pm_luxury_furniture
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_furniture_manufacturies
   production_method = pm_luxury_furniture
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_furniture_manufacturies
     }
     has_active_production_method = pm_precision_tools
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_furniture_manufacturies
   production_method = pm_precision_tools
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_furniture_manufacturies
     }
     has_active_production_method = pm_automation_disabled
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_furniture_manufacturies
   production_method = pm_automation_disabled
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_furniture_manufacturies
     }
     has_active_production_method = pm_watertube_boiler_building_furniture_manufacturies
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_furniture_manufacturies
   production_method = pm_watertube_boiler_building_furniture_manufacturies
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_furniture_manufacturies
     }
     has_active_production_method = pm_rotary_valve_engine_building_furniture_manufacturies
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_furniture_manufacturies
   production_method = pm_rotary_valve_engine_building_furniture_manufacturies
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_furniture_manufacturies
     }
     has_active_production_method = pm_assembly_lines_building_furniture_manufacturies
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_furniture_manufacturies
   production_method = pm_assembly_lines_building_furniture_manufacturies
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_furniture_manufacturies
     }
     has_active_production_method = pm_merchant_guilds_building_furniture_manufacturies
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_furniture_manufacturies
   production_method = pm_merchant_guilds_building_furniture_manufacturies
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_furniture_manufacturies
     }
     has_active_production_method = pm_privately_owned_building_furniture_manufacturies
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_furniture_manufacturies
   production_method = pm_privately_owned_building_furniture_manufacturies
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_furniture_manufacturies
     }
     has_active_production_method = pm_publicly_traded_building_furniture_manufacturies
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_furniture_manufacturies
   production_method = pm_publicly_traded_building_furniture_manufacturies
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_furniture_manufacturies
     }
     has_active_production_method = pm_government_run_building_furniture_manufacturies
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_furniture_manufacturies
   production_method = pm_government_run_building_furniture_manufacturies
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_furniture_manufacturies
     }
     has_active_production_method = pm_worker_cooperative_building_furniture_manufacturies
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_furniture_manufacturies
   production_method = pm_worker_cooperative_building_furniture_manufacturies
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_tooling_workshops
     }
     has_active_production_method = pm_crude_tools
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_tooling_workshops
   production_method = pm_crude_tools
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_tooling_workshops
     }
     has_active_production_method = pm_pig_iron
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_tooling_workshops
   production_method = pm_pig_iron
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_tooling_workshops
     }
     has_active_production_method = pm_steel
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_tooling_workshops
   production_method = pm_steel
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_tooling_workshops
     }
     has_active_production_method = pm_automation_disabled
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_tooling_workshops
   production_method = pm_automation_disabled
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_tooling_workshops
     }
     has_active_production_method = pm_watertube_boiler_building_tooling_workshops
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_tooling_workshops
   production_method = pm_watertube_boiler_building_tooling_workshops
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_tooling_workshops
     }
     has_active_production_method = pm_rotary_valve_engine_building_tooling_workshops
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_tooling_workshops
   production_method = pm_rotary_valve_engine_building_tooling_workshops
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_tooling_workshops
     }
     has_active_production_method = pm_assembly_lines_building_tooling_workshops
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_tooling_workshops
   production_method = pm_assembly_lines_building_tooling_workshops
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_tooling_workshops
     }
     has_active_production_method = pm_merchant_guilds_building_tooling_workshops
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_tooling_workshops
   production_method = pm_merchant_guilds_building_tooling_workshops
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_tooling_workshops
     }
     has_active_production_method = pm_privately_owned_building_tooling_workshops
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_tooling_workshops
   production_method = pm_privately_owned_building_tooling_workshops
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_tooling_workshops
     }
     has_active_production_method = pm_publicly_traded_building_tooling_workshops
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_tooling_workshops
   production_method = pm_publicly_traded_building_tooling_workshops
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_tooling_workshops
     }
     has_active_production_method = pm_government_run_building_tooling_workshops
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_tooling_workshops
   production_method = pm_government_run_building_tooling_workshops
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_tooling_workshops
     }
     has_active_production_method = pm_worker_cooperative_building_tooling_workshops
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_tooling_workshops
   production_method = pm_worker_cooperative_building_tooling_workshops
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_glassworks
     }
     has_active_production_method = pm_forest_glass
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_glassworks
   production_method = pm_forest_glass
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_glassworks
     }
     has_active_production_method = pm_leaded_glass
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_glassworks
   production_method = pm_leaded_glass
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_glassworks
     }
     has_active_production_method = pm_crystal_glass
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_glassworks
   production_method = pm_crystal_glass
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_glassworks
     }
     has_active_production_method = pm_houseware_plastics
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_glassworks
   production_method = pm_houseware_plastics
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_glassworks
     }
     has_active_production_method = pm_disabled_ceramics
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_glassworks
   production_method = pm_disabled_ceramics
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_glassworks
     }
     has_active_production_method = pm_ceramics
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_glassworks
   production_method = pm_ceramics
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_glassworks
     }
     has_active_production_method = pm_bone_china
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_glassworks
   production_method = pm_bone_china
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_glassworks
     }
     has_active_production_method = pm_manual_glassblowing
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_glassworks
   production_method = pm_manual_glassblowing
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_glassworks
     }
     has_active_production_method = pm_automatic_bottle_blowers
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_glassworks
   production_method = pm_automatic_bottle_blowers
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_glassworks
     }
     has_active_production_method = pm_merchant_guilds_building_glassworks
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_glassworks
   production_method = pm_merchant_guilds_building_glassworks
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_glassworks
     }
     has_active_production_method = pm_privately_owned_building_glassworks
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_glassworks
   production_method = pm_privately_owned_building_glassworks
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_glassworks
     }
     has_active_production_method = pm_publicly_traded_building_glassworks
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_glassworks
   production_method = pm_publicly_traded_building_glassworks
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_glassworks
     }
     has_active_production_method = pm_government_run_building_glassworks
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_glassworks
   production_method = pm_government_run_building_glassworks
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_glassworks
     }
     has_active_production_method = pm_worker_cooperative_building_glassworks
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_glassworks
   production_method = pm_worker_cooperative_building_glassworks
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_paper_mills
     }
     has_active_production_method = pm_pulp_pressing
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_paper_mills
   production_method = pm_pulp_pressing
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_paper_mills
     }
     has_active_production_method = pm_sulfite_pulping
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_paper_mills
   production_method = pm_sulfite_pulping
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_paper_mills
     }
     has_active_production_method = pm_bleached_paper
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_paper_mills
   production_method = pm_bleached_paper
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_paper_mills
     }
     has_active_production_method = pm_automation_disabled
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_paper_mills
   production_method = pm_automation_disabled
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_paper_mills
     }
     has_active_production_method = pm_watertube_boiler_building_paper_mills
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_paper_mills
   production_method = pm_watertube_boiler_building_paper_mills
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_paper_mills
     }
     has_active_production_method = pm_rotary_valve_engine_building_paper_mills
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_paper_mills
   production_method = pm_rotary_valve_engine_building_paper_mills
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_paper_mills
     }
     has_active_production_method = pm_merchant_guilds_building_paper_mills
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_paper_mills
   production_method = pm_merchant_guilds_building_paper_mills
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_paper_mills
     }
     has_active_production_method = pm_privately_owned_building_paper_mills
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_paper_mills
   production_method = pm_privately_owned_building_paper_mills
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_paper_mills
     }
     has_active_production_method = pm_publicly_traded_building_paper_mills
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_paper_mills
   production_method = pm_publicly_traded_building_paper_mills
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_paper_mills
     }
     has_active_production_method = pm_government_run_building_paper_mills
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_paper_mills
   production_method = pm_government_run_building_paper_mills
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_paper_mills
     }
     has_active_production_method = pm_worker_cooperative_building_paper_mills
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_paper_mills
   production_method = pm_worker_cooperative_building_paper_mills
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_chemical_plants
     }
     has_active_production_method = pm_artificial_fertilizers
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_chemical_plants
   production_method = pm_artificial_fertilizers
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_chemical_plants
     }
     has_active_production_method = pm_improved_fertilizer
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_chemical_plants
   production_method = pm_improved_fertilizer
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_chemical_plants
     }
     has_active_production_method = pm_nitrogen_fixation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_chemical_plants
   production_method = pm_nitrogen_fixation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_chemical_plants
     }
     has_active_production_method = pm_no_explosives_production
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_chemical_plants
   production_method = pm_no_explosives_production
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_chemical_plants
     }
     has_active_production_method = pm_leblanc_process
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_chemical_plants
   production_method = pm_leblanc_process
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_chemical_plants
     }
     has_active_production_method = pm_ammonia-soda_process
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_chemical_plants
   production_method = pm_ammonia-soda_process
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_chemical_plants
     }
     has_active_production_method = pm_vacuum_evaporation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_chemical_plants
   production_method = pm_vacuum_evaporation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_chemical_plants
     }
     has_active_production_method = pm_brine_electrolysis
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_chemical_plants
   production_method = pm_brine_electrolysis
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_chemical_plants
     }
     has_active_production_method = pm_privately_owned_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_chemical_plants
   production_method = pm_privately_owned_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_chemical_plants
     }
     has_active_production_method = pm_publicly_traded_building_chemical_plants
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_chemical_plants
   production_method = pm_publicly_traded_building_chemical_plants
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_chemical_plants
     }
     has_active_production_method = pm_government_run_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_chemical_plants
   production_method = pm_government_run_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_chemical_plants
     }
     has_active_production_method = pm_worker_cooperative_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_chemical_plants
   production_method = pm_worker_cooperative_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_synthetics_plants
     }
     has_active_production_method = pm_dye_production
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_synthetics_plants
   production_method = pm_dye_production
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_synthetics_plants
     }
     has_active_production_method = pm_no_artificial_fibers
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_synthetics_plants
   production_method = pm_no_artificial_fibers
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_synthetics_plants
     }
     has_active_production_method = pm_rayon
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_synthetics_plants
   production_method = pm_rayon
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_synthetics_plants
     }
     has_active_production_method = pm_privately_owned_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_synthetics_plants
   production_method = pm_privately_owned_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_synthetics_plants
     }
     has_active_production_method = pm_publicly_traded_building_synthetic_plants
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_synthetics_plants
   production_method = pm_publicly_traded_building_synthetic_plants
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_synthetics_plants
     }
     has_active_production_method = pm_government_run_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_synthetics_plants
   production_method = pm_government_run_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_synthetics_plants
     }
     has_active_production_method = pm_worker_cooperative_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_synthetics_plants
   production_method = pm_worker_cooperative_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_steel_mills
     }
     has_active_production_method = pm_blister_steel_process
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_steel_mills
   production_method = pm_blister_steel_process
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_steel_mills
     }
     has_active_production_method = pm_bessemer_process
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_steel_mills
   production_method = pm_bessemer_process
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_steel_mills
     }
     has_active_production_method = pm_open_hearth_process
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_steel_mills
   production_method = pm_open_hearth_process
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_steel_mills
     }
     has_active_production_method = pm_electric_arc_process
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_steel_mills
   production_method = pm_electric_arc_process
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_steel_mills
     }
     has_active_production_method = pm_automation_disabled
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_steel_mills
   production_method = pm_automation_disabled
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_steel_mills
     }
     has_active_production_method = pm_watertube_boiler_building_steel_mills
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_steel_mills
   production_method = pm_watertube_boiler_building_steel_mills
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_steel_mills
     }
     has_active_production_method = pm_rotary_valve_engine_building_steel_mills
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_steel_mills
   production_method = pm_rotary_valve_engine_building_steel_mills
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_steel_mills
     }
     has_active_production_method = pm_privately_owned_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_steel_mills
   production_method = pm_privately_owned_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_steel_mills
     }
     has_active_production_method = pm_publicly_traded_building_steel_mills
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_steel_mills
   production_method = pm_publicly_traded_building_steel_mills
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_steel_mills
     }
     has_active_production_method = pm_government_run_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_steel_mills
   production_method = pm_government_run_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_steel_mills
     }
     has_active_production_method = pm_worker_cooperative_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_steel_mills
   production_method = pm_worker_cooperative_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_motor_industry
     }
     has_active_production_method = pm_steam_engines
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_motor_industry
   production_method = pm_steam_engines
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_motor_industry
     }
     has_active_production_method = pm_electric_engines
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_motor_industry
   production_method = pm_electric_engines
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_motor_industry
     }
     has_active_production_method = pm_diesel_engines
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_motor_industry
   production_method = pm_diesel_engines
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_motor_industry
     }
     has_active_production_method = pm_automobiles_disabled
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_motor_industry
   production_method = pm_automobiles_disabled
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_motor_industry
     }
     has_active_production_method = pm_automobile_production
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_motor_industry
   production_method = pm_automobile_production
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_motor_industry
     }
     has_active_production_method = pm_automation_disabled
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_motor_industry
   production_method = pm_automation_disabled
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_motor_industry
     }
     has_active_production_method = pm_watertube_boiler_building_motor_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_motor_industry
   production_method = pm_watertube_boiler_building_motor_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_motor_industry
     }
     has_active_production_method = pm_rotary_valve_engine_building_motor_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_motor_industry
   production_method = pm_rotary_valve_engine_building_motor_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_motor_industry
     }
     has_active_production_method = pm_assembly_lines_building_motor_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_motor_industry
   production_method = pm_assembly_lines_building_motor_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_motor_industry
     }
     has_active_production_method = pm_privately_owned_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_motor_industry
   production_method = pm_privately_owned_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_motor_industry
     }
     has_active_production_method = pm_publicly_traded_building_motor_industries
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_motor_industry
   production_method = pm_publicly_traded_building_motor_industries
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_motor_industry
     }
     has_active_production_method = pm_government_run_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_motor_industry
   production_method = pm_government_run_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_motor_industry
     }
     has_active_production_method = pm_worker_cooperative_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_motor_industry
   production_method = pm_worker_cooperative_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_shipyards
     }
     has_active_production_method = pm_basic_shipbuilding
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_shipyards
   production_method = pm_basic_shipbuilding
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_shipyards
     }
     has_active_production_method = pm_complex_shipbuilding
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_shipyards
   production_method = pm_complex_shipbuilding
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_shipyards
     }
     has_active_production_method = pm_metal_shipbuilding
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_shipyards
   production_method = pm_metal_shipbuilding
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_shipyards
     }
     has_active_production_method = pm_arc_welding_shipbuilding
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_shipyards
   production_method = pm_arc_welding_shipbuilding
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_shipyards
     }
     has_active_production_method = pm_no_military_shipbuilding
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_shipyards
   production_method = pm_no_military_shipbuilding
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_shipyards
     }
     has_active_production_method = pm_military_shipbuilding_wooden
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_shipyards
   production_method = pm_military_shipbuilding_wooden
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_shipyards
     }
     has_active_production_method = pm_military_shipbuilding_wooden_2
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_shipyards
   production_method = pm_military_shipbuilding_wooden_2
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_shipyards
     }
     has_active_production_method = pm_military_shipbuilding_steam
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_shipyards
   production_method = pm_military_shipbuilding_steam
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_shipyards
     }
     has_active_production_method = pm_military_shipbuilding_steam_2
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_shipyards
   production_method = pm_military_shipbuilding_steam_2
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_shipyards
     }
     has_active_production_method = pm_merchant_guilds_building_shipyards
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_shipyards
   production_method = pm_merchant_guilds_building_shipyards
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_shipyards
     }
     has_active_production_method = pm_privately_owned_building_shipyards
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_shipyards
   production_method = pm_privately_owned_building_shipyards
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_shipyards
     }
     has_active_production_method = pm_publicly_traded_building_shipyards
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_shipyards
   production_method = pm_publicly_traded_building_shipyards
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_shipyards
     }
     has_active_production_method = pm_government_run_building_shipyards
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_shipyards
   production_method = pm_government_run_building_shipyards
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_shipyards
     }
     has_active_production_method = pm_worker_cooperative_building_shipyards
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_shipyards
   production_method = pm_worker_cooperative_building_shipyards
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_war_machine_industry
     }
     has_active_production_method = pm_aeroplane_production
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_war_machine_industry
   production_method = pm_aeroplane_production
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_war_machine_industry
     }
     has_active_production_method = pm_no_tank_production
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_war_machine_industry
   production_method = pm_no_tank_production
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_war_machine_industry
     }
     has_active_production_method = pm_tank_production
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_war_machine_industry
   production_method = pm_tank_production
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_war_machine_industry
     }
     has_active_production_method = pm_privately_owned_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_war_machine_industry
   production_method = pm_privately_owned_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_war_machine_industry
     }
     has_active_production_method = pm_publicly_traded_building_war_machines_induestries
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_war_machine_industry
   production_method = pm_publicly_traded_building_war_machines_induestries
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_war_machine_industry
     }
     has_active_production_method = pm_government_run_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_war_machine_industry
   production_method = pm_government_run_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_war_machine_industry
     }
     has_active_production_method = pm_worker_cooperative_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_war_machine_industry
   production_method = pm_worker_cooperative_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_electrics_industry
     }
     has_active_production_method = pm_telephones
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_electrics_industry
   production_method = pm_telephones
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_electrics_industry
     }
     has_active_production_method = pm_no_radios
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_electrics_industry
   production_method = pm_no_radios
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_electrics_industry
     }
     has_active_production_method = pm_radios
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_electrics_industry
   production_method = pm_radios
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_electrics_industry
     }
     has_active_production_method = pm_privately_owned_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_electrics_industry
   production_method = pm_privately_owned_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_electrics_industry
     }
     has_active_production_method = pm_publicly_traded_building_electrics_industries
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_electrics_industry
   production_method = pm_publicly_traded_building_electrics_industries
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_electrics_industry
     }
     has_active_production_method = pm_government_run_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_electrics_industry
   production_method = pm_government_run_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_electrics_industry
     }
     has_active_production_method = pm_worker_cooperative_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_electrics_industry
   production_method = pm_worker_cooperative_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_arms_industry
     }
     has_active_production_method = pm_muskets
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_arms_industry
   production_method = pm_muskets
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_arms_industry
     }
     has_active_production_method = pm_rifles
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_arms_industry
   production_method = pm_rifles
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_arms_industry
     }
     has_active_production_method = pm_repeaters
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_arms_industry
   production_method = pm_repeaters
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_arms_industry
     }
     has_active_production_method = pm_bolt_action_rifles
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_arms_industry
   production_method = pm_bolt_action_rifles
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_arms_industry
     }
     has_active_production_method = pm_no_artillery_production
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_arms_industry
   production_method = pm_no_artillery_production
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_arms_industry
     }
     has_active_production_method = pm_cannons
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_arms_industry
   production_method = pm_cannons
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_arms_industry
     }
     has_active_production_method = pm_smoothbores
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_arms_industry
   production_method = pm_smoothbores
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_arms_industry
     }
     has_active_production_method = pm_breech_loaders
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_arms_industry
   production_method = pm_breech_loaders
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_arms_industry
     }
     has_active_production_method = pm_merchant_guilds_building_arms_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_arms_industry
   production_method = pm_merchant_guilds_building_arms_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_arms_industry
     }
     has_active_production_method = pm_privately_owned_building_arms_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_arms_industry
   production_method = pm_privately_owned_building_arms_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_arms_industry
     }
     has_active_production_method = pm_publicly_traded_building_arms_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_arms_industry
   production_method = pm_publicly_traded_building_arms_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_arms_industry
     }
     has_active_production_method = pm_government_run_building_arms_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_arms_industry
   production_method = pm_government_run_building_arms_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_arms_industry
     }
     has_active_production_method = pm_worker_cooperative_building_arms_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_arms_industry
   production_method = pm_worker_cooperative_building_arms_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_munition_plants
     }
     has_active_production_method = pm_percussion_caps
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_munition_plants
   production_method = pm_percussion_caps
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_munition_plants
     }
     has_active_production_method = pm_explosive_shells
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_munition_plants
   production_method = pm_explosive_shells
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_munition_plants
     }
     has_active_production_method = pm_privately_owned_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_munition_plants
   production_method = pm_privately_owned_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_munition_plants
     }
     has_active_production_method = pm_publicly_traded_building_munition_plants
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_munition_plants
   production_method = pm_publicly_traded_building_munition_plants
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_munition_plants
     }
     has_active_production_method = pm_government_run_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_munition_plants
   production_method = pm_government_run_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_munition_plants
     }
     has_active_production_method = pm_worker_cooperative_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_munition_plants
   production_method = pm_worker_cooperative_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rye_farm
     }
     has_active_production_method = pm_simple_farming
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rye_farm
   production_method = pm_simple_farming
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rye_farm
     }
     has_active_production_method = pm_soil_enriching_farming
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rye_farm
   production_method = pm_soil_enriching_farming
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rye_farm
     }
     has_active_production_method = pm_fertilization
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rye_farm
   production_method = pm_fertilization
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rye_farm
     }
     has_active_production_method = pm_chemical_fertilizer
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rye_farm
   production_method = pm_chemical_fertilizer
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rye_farm
     }
     has_active_production_method = pm_no_secondary
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rye_farm
   production_method = pm_no_secondary
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rye_farm
     }
     has_active_production_method = pm_potatoes
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rye_farm
   production_method = pm_potatoes
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rye_farm
     }
     has_active_production_method = pm_apple_orchards
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rye_farm
   production_method = pm_apple_orchards
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rye_farm
     }
     has_active_production_method = pm_tools_disabled
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rye_farm
   production_method = pm_tools_disabled
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rye_farm
     }
     has_active_production_method = pm_tools
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rye_farm
   production_method = pm_tools
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rye_farm
     }
     has_active_production_method = pm_steam_threshers
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rye_farm
   production_method = pm_steam_threshers
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rye_farm
     }
     has_active_production_method = pm_tractors
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rye_farm
   production_method = pm_tractors
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rye_farm
     }
     has_active_production_method = pm_compression_ignition_tractors
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rye_farm
   production_method = pm_compression_ignition_tractors
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rye_farm
     }
     has_active_production_method = pm_privately_owned
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rye_farm
   production_method = pm_privately_owned
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rye_farm
     }
     has_active_production_method = pm_publicly_traded
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rye_farm
   production_method = pm_publicly_traded
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rye_farm
     }
     has_active_production_method = pm_government_run
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rye_farm
   production_method = pm_government_run
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rye_farm
     }
     has_active_production_method = pm_worker_cooperative_farm
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rye_farm
   production_method = pm_worker_cooperative_farm
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_wheat_farm
     }
     has_active_production_method = pm_simple_farming
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_wheat_farm
   production_method = pm_simple_farming
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_wheat_farm
     }
     has_active_production_method = pm_soil_enriching_farming
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_wheat_farm
   production_method = pm_soil_enriching_farming
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_wheat_farm
     }
     has_active_production_method = pm_fertilization
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_wheat_farm
   production_method = pm_fertilization
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_wheat_farm
     }
     has_active_production_method = pm_chemical_fertilizer
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_wheat_farm
   production_method = pm_chemical_fertilizer
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_wheat_farm
     }
     has_active_production_method = pm_no_secondary
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_wheat_farm
   production_method = pm_no_secondary
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_wheat_farm
     }
     has_active_production_method = pm_citrus_orchards
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_wheat_farm
   production_method = pm_citrus_orchards
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_wheat_farm
     }
     has_active_production_method = pm_vineyards
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_wheat_farm
   production_method = pm_vineyards
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_wheat_farm
     }
     has_active_production_method = pm_tools_disabled
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_wheat_farm
   production_method = pm_tools_disabled
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_wheat_farm
     }
     has_active_production_method = pm_tools
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_wheat_farm
   production_method = pm_tools
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_wheat_farm
     }
     has_active_production_method = pm_steam_threshers
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_wheat_farm
   production_method = pm_steam_threshers
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_wheat_farm
     }
     has_active_production_method = pm_tractors
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_wheat_farm
   production_method = pm_tractors
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_wheat_farm
     }
     has_active_production_method = pm_compression_ignition_tractors
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_wheat_farm
   production_method = pm_compression_ignition_tractors
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_wheat_farm
     }
     has_active_production_method = pm_privately_owned
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_wheat_farm
   production_method = pm_privately_owned
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_wheat_farm
     }
     has_active_production_method = pm_publicly_traded
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_wheat_farm
   production_method = pm_publicly_traded
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_wheat_farm
     }
     has_active_production_method = pm_government_run
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_wheat_farm
   production_method = pm_government_run
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_wheat_farm
     }
     has_active_production_method = pm_worker_cooperative_farm
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_wheat_farm
   production_method = pm_worker_cooperative_farm
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rice_farm
     }
     has_active_production_method = pm_simple_farming_building_rice_farm
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rice_farm
   production_method = pm_simple_farming_building_rice_farm
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rice_farm
     }
     has_active_production_method = pm_soil_enriching_farming_building_rice_farm
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rice_farm
   production_method = pm_soil_enriching_farming_building_rice_farm
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rice_farm
     }
     has_active_production_method = pm_fertilization_building_rice_farm
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rice_farm
   production_method = pm_fertilization_building_rice_farm
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rice_farm
     }
     has_active_production_method = pm_chemical_fertilizer_building_rice_farm
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rice_farm
   production_method = pm_chemical_fertilizer_building_rice_farm
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rice_farm
     }
     has_active_production_method = pm_no_secondary
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rice_farm
   production_method = pm_no_secondary
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rice_farm
     }
     has_active_production_method = pm_fig_orchards
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rice_farm
   production_method = pm_fig_orchards
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rice_farm
     }
     has_active_production_method = pm_tools_disabled
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rice_farm
   production_method = pm_tools_disabled
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rice_farm
     }
     has_active_production_method = pm_tools
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rice_farm
   production_method = pm_tools
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rice_farm
     }
     has_active_production_method = pm_steam_threshers
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rice_farm
   production_method = pm_steam_threshers
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rice_farm
     }
     has_active_production_method = pm_privately_owned
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rice_farm
   production_method = pm_privately_owned
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rice_farm
     }
     has_active_production_method = pm_publicly_traded
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rice_farm
   production_method = pm_publicly_traded
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rice_farm
     }
     has_active_production_method = pm_government_run
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rice_farm
   production_method = pm_government_run
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rice_farm
     }
     has_active_production_method = pm_worker_cooperative_farm
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rice_farm
   production_method = pm_worker_cooperative_farm
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_maize_farm
     }
     has_active_production_method = pm_simple_farming
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_maize_farm
   production_method = pm_simple_farming
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_maize_farm
     }
     has_active_production_method = pm_soil_enriching_farming
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_maize_farm
   production_method = pm_soil_enriching_farming
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_maize_farm
     }
     has_active_production_method = pm_fertilization
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_maize_farm
   production_method = pm_fertilization
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_maize_farm
     }
     has_active_production_method = pm_chemical_fertilizer
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_maize_farm
   production_method = pm_chemical_fertilizer
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_maize_farm
     }
     has_active_production_method = pm_no_secondary
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_maize_farm
   production_method = pm_no_secondary
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_maize_farm
     }
     has_active_production_method = pm_citrus_orchards
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_maize_farm
   production_method = pm_citrus_orchards
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_maize_farm
     }
     has_active_production_method = pm_vineyards_building_maize_farm
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_maize_farm
   production_method = pm_vineyards_building_maize_farm
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_maize_farm
     }
     has_active_production_method = pm_tools_disabled
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_maize_farm
   production_method = pm_tools_disabled
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_maize_farm
     }
     has_active_production_method = pm_tools
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_maize_farm
   production_method = pm_tools
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_maize_farm
     }
     has_active_production_method = pm_steam_threshers
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_maize_farm
   production_method = pm_steam_threshers
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_maize_farm
     }
     has_active_production_method = pm_tractors
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_maize_farm
   production_method = pm_tractors
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_maize_farm
     }
     has_active_production_method = pm_compression_ignition_tractors
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_maize_farm
   production_method = pm_compression_ignition_tractors
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_maize_farm
     }
     has_active_production_method = pm_privately_owned
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_maize_farm
   production_method = pm_privately_owned
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_maize_farm
     }
     has_active_production_method = pm_publicly_traded
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_maize_farm
   production_method = pm_publicly_traded
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_maize_farm
     }
     has_active_production_method = pm_government_run
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_maize_farm
   production_method = pm_government_run
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_maize_farm
     }
     has_active_production_method = pm_worker_cooperative_farm
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_maize_farm
   production_method = pm_worker_cooperative_farm
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_millet_farm
     }
     has_active_production_method = pm_simple_farming
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_millet_farm
   production_method = pm_simple_farming
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_millet_farm
     }
     has_active_production_method = pm_soil_enriching_farming
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_millet_farm
   production_method = pm_soil_enriching_farming
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_millet_farm
     }
     has_active_production_method = pm_fertilization
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_millet_farm
   production_method = pm_fertilization
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_millet_farm
     }
     has_active_production_method = pm_chemical_fertilizer
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_millet_farm
   production_method = pm_chemical_fertilizer
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_millet_farm
     }
     has_active_production_method = pm_no_secondary
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_millet_farm
   production_method = pm_no_secondary
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_millet_farm
     }
     has_active_production_method = pm_fig_orchards
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_millet_farm
   production_method = pm_fig_orchards
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_millet_farm
     }
     has_active_production_method = pm_tools_disabled
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_millet_farm
   production_method = pm_tools_disabled
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_millet_farm
     }
     has_active_production_method = pm_tools
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_millet_farm
   production_method = pm_tools
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_millet_farm
     }
     has_active_production_method = pm_steam_threshers
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_millet_farm
   production_method = pm_steam_threshers
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_millet_farm
     }
     has_active_production_method = pm_tractors
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_millet_farm
   production_method = pm_tractors
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_millet_farm
     }
     has_active_production_method = pm_compression_ignition_tractors
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_millet_farm
   production_method = pm_compression_ignition_tractors
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_millet_farm
     }
     has_active_production_method = pm_privately_owned
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_millet_farm
   production_method = pm_privately_owned
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_millet_farm
     }
     has_active_production_method = pm_publicly_traded
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_millet_farm
   production_method = pm_publicly_traded
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_millet_farm
     }
     has_active_production_method = pm_government_run
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_millet_farm
   production_method = pm_government_run
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_millet_farm
     }
     has_active_production_method = pm_worker_cooperative_farm
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_millet_farm
   production_method = pm_worker_cooperative_farm
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_livestock_ranch
     }
     has_active_production_method = pm_simple_ranch
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_livestock_ranch
   production_method = pm_simple_ranch
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_livestock_ranch
     }
     has_active_production_method = pm_intensive_grazing_ranch
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_livestock_ranch
   production_method = pm_intensive_grazing_ranch
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_livestock_ranch
     }
     has_active_production_method = pm_open_air_stockyards
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_livestock_ranch
   production_method = pm_open_air_stockyards
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_livestock_ranch
     }
     has_active_production_method = pm_butchering_tools
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_livestock_ranch
   production_method = pm_butchering_tools
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_livestock_ranch
     }
     has_active_production_method = pm_slaughterhouses
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_livestock_ranch
   production_method = pm_slaughterhouses
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_livestock_ranch
     }
     has_active_production_method = pm_mechanized_slaughtering
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_livestock_ranch
   production_method = pm_mechanized_slaughtering
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_livestock_ranch
     }
     has_active_production_method = pm_standard_fences
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_livestock_ranch
   production_method = pm_standard_fences
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_livestock_ranch
     }
     has_active_production_method = pm_barbed_wire_fences
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_livestock_ranch
   production_method = pm_barbed_wire_fences
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_livestock_ranch
     }
     has_active_production_method = pm_electric_fencing
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_livestock_ranch
   production_method = pm_electric_fencing
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_livestock_ranch
     }
     has_active_production_method = pm_unrefrigerated
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_livestock_ranch
   production_method = pm_unrefrigerated
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_livestock_ranch
     }
     has_active_production_method = pm_refrigerated_storage_building_livestock_ranch
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_livestock_ranch
   production_method = pm_refrigerated_storage_building_livestock_ranch
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_livestock_ranch
     }
     has_active_production_method = pm_refrigerated_rail_cars_building_livestock_ranch
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_livestock_ranch
   production_method = pm_refrigerated_rail_cars_building_livestock_ranch
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_livestock_ranch
     }
     has_active_production_method = pm_privately_owned
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_livestock_ranch
   production_method = pm_privately_owned
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_livestock_ranch
     }
     has_active_production_method = pm_publicly_traded
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_livestock_ranch
   production_method = pm_publicly_traded
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_livestock_ranch
     }
     has_active_production_method = pm_government_run
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_livestock_ranch
   production_method = pm_government_run
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_livestock_ranch
     }
     has_active_production_method = pm_worker_cooperative_farm
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_livestock_ranch
   production_method = pm_worker_cooperative_farm
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_coal_mine
     }
     has_active_production_method = pm_picks_and_shovels_building_coal_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_coal_mine
   production_method = pm_picks_and_shovels_building_coal_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_coal_mine
     }
     has_active_production_method = pm_atmospheric_engine_pump_building_coal_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_coal_mine
   production_method = pm_atmospheric_engine_pump_building_coal_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_coal_mine
     }
     has_active_production_method = pm_condensing_engine_pump_building_coal_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_coal_mine
   production_method = pm_condensing_engine_pump_building_coal_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_coal_mine
     }
     has_active_production_method = pm_diesel_pump_building_coal_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_coal_mine
   production_method = pm_diesel_pump_building_coal_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_coal_mine
     }
     has_active_production_method = pm_no_explosives
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_coal_mine
   production_method = pm_no_explosives
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_coal_mine
     }
     has_active_production_method = pm_nitroglycerin_building_coal_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_coal_mine
   production_method = pm_nitroglycerin_building_coal_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_coal_mine
     }
     has_active_production_method = pm_dynamite_building_coal_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_coal_mine
   production_method = pm_dynamite_building_coal_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_coal_mine
     }
     has_active_production_method = pm_no_steam_automation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_coal_mine
   production_method = pm_no_steam_automation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_coal_mine
     }
     has_active_production_method = pm_steam_donkey_building_coal_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_coal_mine
   production_method = pm_steam_donkey_building_coal_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_coal_mine
     }
     has_active_production_method = pm_road_carts
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_coal_mine
   production_method = pm_road_carts
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_coal_mine
     }
     has_active_production_method = pm_rail_transport_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_coal_mine
   production_method = pm_rail_transport_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_coal_mine
     }
     has_active_production_method = pm_merchant_guilds_building_coal_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_coal_mine
   production_method = pm_merchant_guilds_building_coal_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_coal_mine
     }
     has_active_production_method = pm_privately_owned_building_coal_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_coal_mine
   production_method = pm_privately_owned_building_coal_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_coal_mine
     }
     has_active_production_method = pm_publicly_traded_building_coal_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_coal_mine
   production_method = pm_publicly_traded_building_coal_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_coal_mine
     }
     has_active_production_method = pm_government_run_building_coal_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_coal_mine
   production_method = pm_government_run_building_coal_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_coal_mine
     }
     has_active_production_method = pm_worker_cooperative_building_coal_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_coal_mine
   production_method = pm_worker_cooperative_building_coal_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_iron_mine
     }
     has_active_production_method = pm_picks_and_shovels_building_iron_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_iron_mine
   production_method = pm_picks_and_shovels_building_iron_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_iron_mine
     }
     has_active_production_method = pm_atmospheric_engine_pump_building_iron_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_iron_mine
   production_method = pm_atmospheric_engine_pump_building_iron_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_iron_mine
     }
     has_active_production_method = pm_condensing_engine_pump_building_iron_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_iron_mine
   production_method = pm_condensing_engine_pump_building_iron_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_iron_mine
     }
     has_active_production_method = pm_diesel_pump_building_iron_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_iron_mine
   production_method = pm_diesel_pump_building_iron_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_iron_mine
     }
     has_active_production_method = pm_no_explosives
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_iron_mine
   production_method = pm_no_explosives
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_iron_mine
     }
     has_active_production_method = pm_nitroglycerin_building_iron_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_iron_mine
   production_method = pm_nitroglycerin_building_iron_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_iron_mine
     }
     has_active_production_method = pm_dynamite_building_iron_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_iron_mine
   production_method = pm_dynamite_building_iron_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_iron_mine
     }
     has_active_production_method = pm_no_steam_automation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_iron_mine
   production_method = pm_no_steam_automation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_iron_mine
     }
     has_active_production_method = pm_steam_donkey_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_iron_mine
   production_method = pm_steam_donkey_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_iron_mine
     }
     has_active_production_method = pm_road_carts
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_iron_mine
   production_method = pm_road_carts
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_iron_mine
     }
     has_active_production_method = pm_rail_transport_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_iron_mine
   production_method = pm_rail_transport_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_iron_mine
     }
     has_active_production_method = pm_merchant_guilds_building_iron_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_iron_mine
   production_method = pm_merchant_guilds_building_iron_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_iron_mine
     }
     has_active_production_method = pm_privately_owned_building_iron_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_iron_mine
   production_method = pm_privately_owned_building_iron_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_iron_mine
     }
     has_active_production_method = pm_publicly_traded_building_iron_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_iron_mine
   production_method = pm_publicly_traded_building_iron_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_iron_mine
     }
     has_active_production_method = pm_government_run_building_iron_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_iron_mine
   production_method = pm_government_run_building_iron_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_iron_mine
     }
     has_active_production_method = pm_worker_cooperative_building_iron_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_iron_mine
   production_method = pm_worker_cooperative_building_iron_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_lead_mine
     }
     has_active_production_method = pm_picks_and_shovels_building_lead_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_lead_mine
   production_method = pm_picks_and_shovels_building_lead_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_lead_mine
     }
     has_active_production_method = pm_atmospheric_engine_pump_building_lead_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_lead_mine
   production_method = pm_atmospheric_engine_pump_building_lead_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_lead_mine
     }
     has_active_production_method = pm_condensing_engine_pump_building_lead_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_lead_mine
   production_method = pm_condensing_engine_pump_building_lead_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_lead_mine
     }
     has_active_production_method = pm_diesel_pump_building_lead_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_lead_mine
   production_method = pm_diesel_pump_building_lead_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_lead_mine
     }
     has_active_production_method = pm_no_explosives
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_lead_mine
   production_method = pm_no_explosives
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_lead_mine
     }
     has_active_production_method = pm_nitroglycerin_building_lead_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_lead_mine
   production_method = pm_nitroglycerin_building_lead_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_lead_mine
     }
     has_active_production_method = pm_dynamite_building_lead_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_lead_mine
   production_method = pm_dynamite_building_lead_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_lead_mine
     }
     has_active_production_method = pm_no_steam_automation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_lead_mine
   production_method = pm_no_steam_automation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_lead_mine
     }
     has_active_production_method = pm_steam_donkey_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_lead_mine
   production_method = pm_steam_donkey_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_lead_mine
     }
     has_active_production_method = pm_road_carts
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_lead_mine
   production_method = pm_road_carts
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_lead_mine
     }
     has_active_production_method = pm_rail_transport_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_lead_mine
   production_method = pm_rail_transport_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_lead_mine
     }
     has_active_production_method = pm_merchant_guilds_building_lead_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_lead_mine
   production_method = pm_merchant_guilds_building_lead_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_lead_mine
     }
     has_active_production_method = pm_privately_owned_building_lead_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_lead_mine
   production_method = pm_privately_owned_building_lead_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_lead_mine
     }
     has_active_production_method = pm_publicly_traded_building_lead_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_lead_mine
   production_method = pm_publicly_traded_building_lead_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_lead_mine
     }
     has_active_production_method = pm_government_run_building_lead_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_lead_mine
   production_method = pm_government_run_building_lead_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_lead_mine
     }
     has_active_production_method = pm_worker_cooperative_building_lead_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_lead_mine
   production_method = pm_worker_cooperative_building_lead_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_sulfur_mine
     }
     has_active_production_method = pm_picks_and_shovels_building_sulfur_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_sulfur_mine
   production_method = pm_picks_and_shovels_building_sulfur_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_sulfur_mine
     }
     has_active_production_method = pm_atmospheric_engine_pump_building_sulfur_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_sulfur_mine
   production_method = pm_atmospheric_engine_pump_building_sulfur_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_sulfur_mine
     }
     has_active_production_method = pm_condensing_engine_pump_building_sulfur_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_sulfur_mine
   production_method = pm_condensing_engine_pump_building_sulfur_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_sulfur_mine
     }
     has_active_production_method = pm_diesel_pump_building_sulfur_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_sulfur_mine
   production_method = pm_diesel_pump_building_sulfur_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_sulfur_mine
     }
     has_active_production_method = pm_no_explosives
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_sulfur_mine
   production_method = pm_no_explosives
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_sulfur_mine
     }
     has_active_production_method = pm_nitroglycerin_building_sulfur_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_sulfur_mine
   production_method = pm_nitroglycerin_building_sulfur_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_sulfur_mine
     }
     has_active_production_method = pm_dynamite_building_sulfur_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_sulfur_mine
   production_method = pm_dynamite_building_sulfur_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_sulfur_mine
     }
     has_active_production_method = pm_no_steam_automation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_sulfur_mine
   production_method = pm_no_steam_automation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_sulfur_mine
     }
     has_active_production_method = pm_steam_donkey_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_sulfur_mine
   production_method = pm_steam_donkey_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_sulfur_mine
     }
     has_active_production_method = pm_road_carts
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_sulfur_mine
   production_method = pm_road_carts
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_sulfur_mine
     }
     has_active_production_method = pm_rail_transport_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_sulfur_mine
   production_method = pm_rail_transport_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_sulfur_mine
     }
     has_active_production_method = pm_merchant_guilds_building_sulfur_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_sulfur_mine
   production_method = pm_merchant_guilds_building_sulfur_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_sulfur_mine
     }
     has_active_production_method = pm_privately_owned_building_sulfur_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_sulfur_mine
   production_method = pm_privately_owned_building_sulfur_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_sulfur_mine
     }
     has_active_production_method = pm_publicly_traded_building_sulfur_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_sulfur_mine
   production_method = pm_publicly_traded_building_sulfur_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_sulfur_mine
     }
     has_active_production_method = pm_government_run_building_sulfur_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_sulfur_mine
   production_method = pm_government_run_building_sulfur_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_sulfur_mine
     }
     has_active_production_method = pm_worker_cooperative_building_sulfur_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_sulfur_mine
   production_method = pm_worker_cooperative_building_sulfur_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_gold_mine
     }
     has_active_production_method = pm_picks_and_shovels_building_gold_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_gold_mine
   production_method = pm_picks_and_shovels_building_gold_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_gold_mine
     }
     has_active_production_method = pm_atmospheric_engine_pump_building_gold_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_gold_mine
   production_method = pm_atmospheric_engine_pump_building_gold_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_gold_mine
     }
     has_active_production_method = pm_condensing_engine_pump_building_gold_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_gold_mine
   production_method = pm_condensing_engine_pump_building_gold_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_gold_mine
     }
     has_active_production_method = pm_diesel_pump_building_gold_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_gold_mine
   production_method = pm_diesel_pump_building_gold_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_gold_mine
     }
     has_active_production_method = pm_no_explosives
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_gold_mine
   production_method = pm_no_explosives
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_gold_mine
     }
     has_active_production_method = pm_nitroglycerin_building_gold_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_gold_mine
   production_method = pm_nitroglycerin_building_gold_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_gold_mine
     }
     has_active_production_method = pm_dynamite_building_gold_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_gold_mine
   production_method = pm_dynamite_building_gold_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_gold_mine
     }
     has_active_production_method = pm_no_steam_automation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_gold_mine
   production_method = pm_no_steam_automation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_gold_mine
     }
     has_active_production_method = pm_steam_donkey_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_gold_mine
   production_method = pm_steam_donkey_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_gold_mine
     }
     has_active_production_method = pm_road_carts
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_gold_mine
   production_method = pm_road_carts
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_gold_mine
     }
     has_active_production_method = pm_rail_transport_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_gold_mine
   production_method = pm_rail_transport_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_gold_mine
     }
     has_active_production_method = pm_merchant_guilds_building_gold_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_gold_mine
   production_method = pm_merchant_guilds_building_gold_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_gold_mine
     }
     has_active_production_method = pm_privately_owned_building_gold_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_gold_mine
   production_method = pm_privately_owned_building_gold_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_gold_mine
     }
     has_active_production_method = pm_publicly_traded_building_gold_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_gold_mine
   production_method = pm_publicly_traded_building_gold_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_gold_mine
     }
     has_active_production_method = pm_government_run_building_gold_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_gold_mine
   production_method = pm_government_run_building_gold_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_gold_mine
     }
     has_active_production_method = pm_worker_cooperative_building_gold_mine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_gold_mine
   production_method = pm_worker_cooperative_building_gold_mine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_coffee_plantation
     }
     has_active_production_method = default_building_coffee_plantation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_coffee_plantation
   production_method = default_building_coffee_plantation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_coffee_plantation
     }
     has_active_production_method = automatic_irrigation_building_coffee_plantation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_coffee_plantation
   production_method = automatic_irrigation_building_coffee_plantation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_coffee_plantation
     }
     has_active_production_method = pm_road_carts
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_coffee_plantation
   production_method = pm_road_carts
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_coffee_plantation
     }
     has_active_production_method = pm_steam_rail_transport
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_coffee_plantation
   production_method = pm_steam_rail_transport
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_coffee_plantation
     }
     has_active_production_method = pm_privately_owned
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_coffee_plantation
   production_method = pm_privately_owned
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_coffee_plantation
     }
     has_active_production_method = pm_publicly_traded
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_coffee_plantation
   production_method = pm_publicly_traded
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_coffee_plantation
     }
     has_active_production_method = pm_government_run
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_coffee_plantation
   production_method = pm_government_run
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_coffee_plantation
     }
     has_active_production_method = pm_worker_cooperative_plantation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_coffee_plantation
   production_method = pm_worker_cooperative_plantation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_cotton_plantation
     }
     has_active_production_method = default_building_cotton_plantation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_cotton_plantation
   production_method = default_building_cotton_plantation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_cotton_plantation
     }
     has_active_production_method = automatic_irrigation_building_cotton_plantation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_cotton_plantation
   production_method = automatic_irrigation_building_cotton_plantation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_cotton_plantation
     }
     has_active_production_method = pm_road_carts
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_cotton_plantation
   production_method = pm_road_carts
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_cotton_plantation
     }
     has_active_production_method = pm_steam_rail_transport
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_cotton_plantation
   production_method = pm_steam_rail_transport
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_cotton_plantation
     }
     has_active_production_method = pm_privately_owned
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_cotton_plantation
   production_method = pm_privately_owned
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_cotton_plantation
     }
     has_active_production_method = pm_publicly_traded
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_cotton_plantation
   production_method = pm_publicly_traded
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_cotton_plantation
     }
     has_active_production_method = pm_government_run
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_cotton_plantation
   production_method = pm_government_run
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_cotton_plantation
     }
     has_active_production_method = pm_worker_cooperative_plantation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_cotton_plantation
   production_method = pm_worker_cooperative_plantation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_dye_plantation
     }
     has_active_production_method = default_building_dye_plantation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_dye_plantation
   production_method = default_building_dye_plantation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_dye_plantation
     }
     has_active_production_method = automatic_irrigation_building_dye_plantation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_dye_plantation
   production_method = automatic_irrigation_building_dye_plantation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_dye_plantation
     }
     has_active_production_method = pm_road_carts
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_dye_plantation
   production_method = pm_road_carts
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_dye_plantation
     }
     has_active_production_method = pm_steam_rail_transport
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_dye_plantation
   production_method = pm_steam_rail_transport
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_dye_plantation
     }
     has_active_production_method = pm_privately_owned
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_dye_plantation
   production_method = pm_privately_owned
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_dye_plantation
     }
     has_active_production_method = pm_publicly_traded
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_dye_plantation
   production_method = pm_publicly_traded
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_dye_plantation
     }
     has_active_production_method = pm_government_run
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_dye_plantation
   production_method = pm_government_run
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_dye_plantation
     }
     has_active_production_method = pm_worker_cooperative_plantation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_dye_plantation
   production_method = pm_worker_cooperative_plantation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_opium_plantation
     }
     has_active_production_method = default_building_opium_plantation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_opium_plantation
   production_method = default_building_opium_plantation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_opium_plantation
     }
     has_active_production_method = automatic_irrigation_building_opium_plantation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_opium_plantation
   production_method = automatic_irrigation_building_opium_plantation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_opium_plantation
     }
     has_active_production_method = pm_road_carts
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_opium_plantation
   production_method = pm_road_carts
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_opium_plantation
     }
     has_active_production_method = pm_steam_rail_transport
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_opium_plantation
   production_method = pm_steam_rail_transport
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_opium_plantation
     }
     has_active_production_method = pm_privately_owned
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_opium_plantation
   production_method = pm_privately_owned
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_opium_plantation
     }
     has_active_production_method = pm_publicly_traded
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_opium_plantation
   production_method = pm_publicly_traded
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_opium_plantation
     }
     has_active_production_method = pm_government_run
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_opium_plantation
   production_method = pm_government_run
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_opium_plantation
     }
     has_active_production_method = pm_worker_cooperative_plantation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_opium_plantation
   production_method = pm_worker_cooperative_plantation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_tea_plantation
     }
     has_active_production_method = default_building_tea_plantation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_tea_plantation
   production_method = default_building_tea_plantation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_tea_plantation
     }
     has_active_production_method = automatic_irrigation_building_tea_plantation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_tea_plantation
   production_method = automatic_irrigation_building_tea_plantation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_tea_plantation
     }
     has_active_production_method = pm_road_carts
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_tea_plantation
   production_method = pm_road_carts
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_tea_plantation
     }
     has_active_production_method = pm_steam_rail_transport
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_tea_plantation
   production_method = pm_steam_rail_transport
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_tea_plantation
     }
     has_active_production_method = pm_privately_owned
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_tea_plantation
   production_method = pm_privately_owned
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_tea_plantation
     }
     has_active_production_method = pm_publicly_traded
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_tea_plantation
   production_method = pm_publicly_traded
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_tea_plantation
     }
     has_active_production_method = pm_government_run
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_tea_plantation
   production_method = pm_government_run
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_tea_plantation
     }
     has_active_production_method = pm_worker_cooperative_plantation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_tea_plantation
   production_method = pm_worker_cooperative_plantation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_tobacco_plantation
     }
     has_active_production_method = default_building_tobacco_plantation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_tobacco_plantation
   production_method = default_building_tobacco_plantation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_tobacco_plantation
     }
     has_active_production_method = automatic_irrigation_building_tobacco_plantation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_tobacco_plantation
   production_method = automatic_irrigation_building_tobacco_plantation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_tobacco_plantation
     }
     has_active_production_method = pm_road_carts
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_tobacco_plantation
   production_method = pm_road_carts
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_tobacco_plantation
     }
     has_active_production_method = pm_steam_rail_transport
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_tobacco_plantation
   production_method = pm_steam_rail_transport
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_tobacco_plantation
     }
     has_active_production_method = pm_privately_owned
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_tobacco_plantation
   production_method = pm_privately_owned
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_tobacco_plantation
     }
     has_active_production_method = pm_publicly_traded
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_tobacco_plantation
   production_method = pm_publicly_traded
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_tobacco_plantation
     }
     has_active_production_method = pm_government_run
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_tobacco_plantation
   production_method = pm_government_run
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_tobacco_plantation
     }
     has_active_production_method = pm_worker_cooperative_plantation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_tobacco_plantation
   production_method = pm_worker_cooperative_plantation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_sugar_plantation
     }
     has_active_production_method = default_building_sugar_plantation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_sugar_plantation
   production_method = default_building_sugar_plantation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_sugar_plantation
     }
     has_active_production_method = automatic_irrigation_building_sugar_plantation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_sugar_plantation
   production_method = automatic_irrigation_building_sugar_plantation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_sugar_plantation
     }
     has_active_production_method = pm_road_carts
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_sugar_plantation
   production_method = pm_road_carts
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_sugar_plantation
     }
     has_active_production_method = pm_steam_rail_transport
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_sugar_plantation
   production_method = pm_steam_rail_transport
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_sugar_plantation
     }
     has_active_production_method = pm_privately_owned
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_sugar_plantation
   production_method = pm_privately_owned
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_sugar_plantation
     }
     has_active_production_method = pm_publicly_traded
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_sugar_plantation
   production_method = pm_publicly_traded
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_sugar_plantation
     }
     has_active_production_method = pm_government_run
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_sugar_plantation
   production_method = pm_government_run
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_sugar_plantation
     }
     has_active_production_method = pm_worker_cooperative_plantation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_sugar_plantation
   production_method = pm_worker_cooperative_plantation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_banana_plantation
     }
     has_active_production_method = default_building_banana_plantation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_banana_plantation
   production_method = default_building_banana_plantation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_banana_plantation
     }
     has_active_production_method = automatic_irrigation_building_banana_plantation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_banana_plantation
   production_method = automatic_irrigation_building_banana_plantation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_banana_plantation
     }
     has_active_production_method = pm_road_carts
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_banana_plantation
   production_method = pm_road_carts
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_banana_plantation
     }
     has_active_production_method = pm_steam_rail_transport
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_banana_plantation
   production_method = pm_steam_rail_transport
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_banana_plantation
     }
     has_active_production_method = pm_privately_owned
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_banana_plantation
   production_method = pm_privately_owned
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_banana_plantation
     }
     has_active_production_method = pm_publicly_traded
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_banana_plantation
   production_method = pm_publicly_traded
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_banana_plantation
     }
     has_active_production_method = pm_government_run
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_banana_plantation
   production_method = pm_government_run
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_banana_plantation
     }
     has_active_production_method = pm_worker_cooperative_plantation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_banana_plantation
   production_method = pm_worker_cooperative_plantation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_silk_plantation
     }
     has_active_production_method = default_building_silk_plantation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_silk_plantation
   production_method = default_building_silk_plantation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_silk_plantation
     }
     has_active_production_method = automatic_irrigation_building_silk_plantation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_silk_plantation
   production_method = automatic_irrigation_building_silk_plantation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_silk_plantation
     }
     has_active_production_method = pm_road_carts
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_silk_plantation
   production_method = pm_road_carts
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_silk_plantation
     }
     has_active_production_method = pm_steam_rail_transport
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_silk_plantation
   production_method = pm_steam_rail_transport
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_silk_plantation
     }
     has_active_production_method = pm_privately_owned
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_silk_plantation
   production_method = pm_privately_owned
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_silk_plantation
     }
     has_active_production_method = pm_publicly_traded
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_silk_plantation
   production_method = pm_publicly_traded
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_silk_plantation
     }
     has_active_production_method = pm_government_run
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_silk_plantation
   production_method = pm_government_run
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_silk_plantation
     }
     has_active_production_method = pm_worker_cooperative_plantation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_silk_plantation
   production_method = pm_worker_cooperative_plantation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_barracks
     }
     has_active_production_method = pm_irregular_infantry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_barracks
   production_method = pm_irregular_infantry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_barracks
     }
     has_active_production_method = pm_line_infantry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_barracks
   production_method = pm_line_infantry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_barracks
     }
     has_active_production_method = pm_skirmish_infantry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_barracks
   production_method = pm_skirmish_infantry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_barracks
     }
     has_active_production_method = pm_trench_infantry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_barracks
   production_method = pm_trench_infantry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_barracks
     }
     has_active_production_method = pm_squad_infantry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_barracks
   production_method = pm_squad_infantry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_barracks
     }
     has_active_production_method = pm_mechanized_infantry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_barracks
   production_method = pm_mechanized_infantry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_barracks
     }
     has_active_production_method = pm_no_artillery
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_barracks
   production_method = pm_no_artillery
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_barracks
     }
     has_active_production_method = pm_cannon_artillery
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_barracks
   production_method = pm_cannon_artillery
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_barracks
     }
     has_active_production_method = pm_mobile_artillery
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_barracks
   production_method = pm_mobile_artillery
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_barracks
     }
     has_active_production_method = pm_shrapnel_artillery
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_barracks
   production_method = pm_shrapnel_artillery
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_barracks
     }
     has_active_production_method = pm_siege_artillery
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_barracks
   production_method = pm_siege_artillery
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_barracks
     }
     has_active_production_method = pm_cavalry_scouts
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_barracks
   production_method = pm_cavalry_scouts
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_barracks
     }
     has_active_production_method = pm_bicycle_messengers
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_barracks
   production_method = pm_bicycle_messengers
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_barracks
     }
     has_active_production_method = pm_motorized_recon
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_barracks
   production_method = pm_motorized_recon
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_barracks
     }
     has_active_production_method = pm_aerial_recon
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_barracks
   production_method = pm_aerial_recon
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_barracks
     }
     has_active_production_method = pm_no_specialists
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_barracks
   production_method = pm_no_specialists
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_barracks
     }
     has_active_production_method = pm_machine_gunners
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_barracks
   production_method = pm_machine_gunners
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_barracks
     }
     has_active_production_method = pm_infiltrators
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_barracks
   production_method = pm_infiltrators
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_barracks
     }
     has_active_production_method = pm_flamethrower_company
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_barracks
   production_method = pm_flamethrower_company
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_barracks
     }
     has_active_production_method = pm_chemical_weapons_specialists
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_barracks
   production_method = pm_chemical_weapons_specialists
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_conscription_center
     }
     has_active_production_method = pm_irregular_infantry_conscription
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_conscription_center
   production_method = pm_irregular_infantry_conscription
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_conscription_center
     }
     has_active_production_method = pm_line_infantry_conscription
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_conscription_center
   production_method = pm_line_infantry_conscription
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_conscription_center
     }
     has_active_production_method = pm_skirmish_infantry_conscription
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_conscription_center
   production_method = pm_skirmish_infantry_conscription
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_conscription_center
     }
     has_active_production_method = pm_trench_infantry_conscription
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_conscription_center
   production_method = pm_trench_infantry_conscription
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_conscription_center
     }
     has_active_production_method = pm_squad_infantry_conscription
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_conscription_center
   production_method = pm_squad_infantry_conscription
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_conscription_center
     }
     has_active_production_method = pm_mechanized_infantry_conscription
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_conscription_center
   production_method = pm_mechanized_infantry_conscription
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_conscription_center
     }
     has_active_production_method = pm_no_artillery_conscription
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_conscription_center
   production_method = pm_no_artillery_conscription
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_conscription_center
     }
     has_active_production_method = pm_cannon_artillery_conscription
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_conscription_center
   production_method = pm_cannon_artillery_conscription
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_conscription_center
     }
     has_active_production_method = pm_mobile_artillery_conscription
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_conscription_center
   production_method = pm_mobile_artillery_conscription
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_conscription_center
     }
     has_active_production_method = pm_shrapnel_artillery_conscription
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_conscription_center
   production_method = pm_shrapnel_artillery_conscription
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_conscription_center
     }
     has_active_production_method = pm_siege_artillery_conscription
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_conscription_center
   production_method = pm_siege_artillery_conscription
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_conscription_center
     }
     has_active_production_method = pm_cavalry_scouts_conscription
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_conscription_center
   production_method = pm_cavalry_scouts_conscription
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_conscription_center
     }
     has_active_production_method = pm_bicycle_messengers_conscription
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_conscription_center
   production_method = pm_bicycle_messengers_conscription
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_conscription_center
     }
     has_active_production_method = pm_motorized_recon_conscription
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_conscription_center
   production_method = pm_motorized_recon_conscription
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_conscription_center
     }
     has_active_production_method = pm_aerial_recon_conscription
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_conscription_center
   production_method = pm_aerial_recon_conscription
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_conscription_center
     }
     has_active_production_method = pm_no_specialists_conscription
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_conscription_center
   production_method = pm_no_specialists_conscription
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_conscription_center
     }
     has_active_production_method = pm_machine_gunners_conscription
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_conscription_center
   production_method = pm_machine_gunners_conscription
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_conscription_center
     }
     has_active_production_method = pm_infiltrators_conscription
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_conscription_center
   production_method = pm_infiltrators_conscription
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_conscription_center
     }
     has_active_production_method = pm_flamethrower_company_conscription
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_conscription_center
   production_method = pm_flamethrower_company_conscription
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_conscription_center
     }
     has_active_production_method = pm_chemical_weapons_specialists_conscription
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_conscription_center
   production_method = pm_chemical_weapons_specialists_conscription
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_conscription_center
     }
     has_active_production_method = pm_wound_dressing
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_conscription_center
   production_method = pm_wound_dressing
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_conscription_center
     }
     has_active_production_method = pm_first_aid
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_conscription_center
   production_method = pm_first_aid
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_conscription_center
     }
     has_active_production_method = pm_field_hospitals
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_conscription_center
   production_method = pm_field_hospitals
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_barracks
     }
     has_active_production_method = pm_wound_dressing
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_barracks
   production_method = pm_wound_dressing
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_barracks
     }
     has_active_production_method = pm_first_aid
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_barracks
   production_method = pm_first_aid
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_barracks
     }
     has_active_production_method = pm_field_hospitals
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_barracks
   production_method = pm_field_hospitals
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_naval_base
     }
     has_active_production_method = pm_man_o_wars
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_naval_base
   production_method = pm_man_o_wars
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_naval_base
     }
     has_active_production_method = pm_ironclads
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_naval_base
   production_method = pm_ironclads
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_naval_base
     }
     has_active_production_method = pm_monitors
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_naval_base
   production_method = pm_monitors
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_naval_base
     }
     has_active_production_method = pm_dreadnoughts
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_naval_base
   production_method = pm_dreadnoughts
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_naval_base
     }
     has_active_production_method = pm_battleships
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_naval_base
   production_method = pm_battleships
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_naval_base
     }
     has_active_production_method = pm_frigates
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_naval_base
   production_method = pm_frigates
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_naval_base
     }
     has_active_production_method = pm_destroyers
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_naval_base
   production_method = pm_destroyers
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_naval_base
     }
     has_active_production_method = pm_carriers
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_naval_base
   production_method = pm_carriers
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_naval_base
     }
     has_active_production_method = pm_commerce_raider
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_naval_base
   production_method = pm_commerce_raider
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_naval_base
     }
     has_active_production_method = pm_torpedo_boat
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_naval_base
   production_method = pm_torpedo_boat
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_naval_base
     }
     has_active_production_method = pm_submarine
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_naval_base
   production_method = pm_submarine
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_naval_base
     }
     has_active_production_method = pm_no_naval_theory
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_naval_base
   production_method = pm_no_naval_theory
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_naval_base
     }
     has_active_production_method = pm_power_of_the_purse
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_naval_base
   production_method = pm_power_of_the_purse
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_naval_base
     }
     has_active_production_method = pm_jeune_ecole
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_naval_base
   production_method = pm_jeune_ecole
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_naval_base
     }
     has_active_production_method = pm_sea_lane_strategies
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_naval_base
   production_method = pm_sea_lane_strategies
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_naval_base
     }
     has_active_production_method = pm_battlefleet_tactics
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_naval_base
   production_method = pm_battlefleet_tactics
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_urban_center
     }
     has_active_production_method = pm_market_stalls
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_urban_center
   production_method = pm_market_stalls
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_urban_center
     }
     has_active_production_method = pm_market_squares
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_urban_center
   production_method = pm_market_squares
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_urban_center
     }
     has_active_production_method = pm_covered_markets
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_urban_center
   production_method = pm_covered_markets
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_urban_center
     }
     has_active_production_method = pm_arcades
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_urban_center
   production_method = pm_arcades
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_urban_center
     }
     has_active_production_method = pm_no_street_lighting
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_urban_center
   production_method = pm_no_street_lighting
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_urban_center
     }
     has_active_production_method = pm_gas_streetlights
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_urban_center
   production_method = pm_gas_streetlights
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_urban_center
     }
     has_active_production_method = pm_electric_streetlights
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_urban_center
   production_method = pm_electric_streetlights
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_urban_center
     }
     has_active_production_method = pm_no_public_transport
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_urban_center
   production_method = pm_no_public_transport
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_urban_center
     }
     has_active_production_method = pm_public_trams
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_urban_center
   production_method = pm_public_trams
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_urban_center
     }
     has_active_production_method = pm_public_motor_carriages
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_urban_center
   production_method = pm_public_motor_carriages
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_urban_center
     }
     has_active_production_method = pm_state_urban_clergy
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_urban_center
   production_method = pm_state_urban_clergy
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_urban_center
     }
     has_active_production_method = pm_free_urban_clergy
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_urban_center
   production_method = pm_free_urban_clergy
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_arts_academy
     }
     has_active_production_method = pm_traditional_art
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_arts_academy
   production_method = pm_traditional_art
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_arts_academy
     }
     has_active_production_method = pm_realist_art
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_arts_academy
   production_method = pm_realist_art
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_arts_academy
     }
     has_active_production_method = pm_photographic_art
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_arts_academy
   production_method = pm_photographic_art
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_arts_academy
     }
     has_active_production_method = pm_film_art
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_arts_academy
   production_method = pm_film_art
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_arts_academy
     }
     has_active_production_method = pm_traditional_patronage
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_arts_academy
   production_method = pm_traditional_patronage
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_arts_academy
     }
     has_active_production_method = pm_bourgeoisie_patronage
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_arts_academy
   production_method = pm_bourgeoisie_patronage
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_arts_academy
     }
     has_active_production_method = pm_independent_artists
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_arts_academy
   production_method = pm_independent_artists
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_power_plant
     }
     has_active_production_method = pm_hydroelectric_plant
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_power_plant
   production_method = pm_hydroelectric_plant
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_power_plant
     }
     has_active_production_method = pm_coal-fired_plant
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_power_plant
   production_method = pm_coal-fired_plant
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_power_plant
     }
     has_active_production_method = pm_oil-fired_plant
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_power_plant
   production_method = pm_oil-fired_plant
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_power_plant
     }
     has_active_production_method = pm_privately_owned_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_power_plant
   production_method = pm_privately_owned_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_power_plant
     }
     has_active_production_method = pm_publicly_traded_building_power_plant
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_power_plant
   production_method = pm_publicly_traded_building_power_plant
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_power_plant
     }
     has_active_production_method = pm_government_run_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_power_plant
   production_method = pm_government_run_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_power_plant
     }
     has_active_production_method = pm_worker_cooperative_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_power_plant
   production_method = pm_worker_cooperative_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_port
     }
     has_active_production_method = pm_anchorage
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_port
   production_method = pm_anchorage
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_port
     }
     has_active_production_method = pm_basic_port
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_port
   production_method = pm_basic_port
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_port
     }
     has_active_production_method = pm_industrial_port
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_port
   production_method = pm_industrial_port
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_port
     }
     has_active_production_method = pm_modern_port
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_port
   production_method = pm_modern_port
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_government_administration
     }
     has_active_production_method = pm_simple_organization
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_government_administration
   production_method = pm_simple_organization
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_government_administration
     }
     has_active_production_method = pm_horizontal_drawer_cabinets
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_government_administration
   production_method = pm_horizontal_drawer_cabinets
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_government_administration
     }
     has_active_production_method = pm_vertical_filing_cabinets
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_government_administration
   production_method = pm_vertical_filing_cabinets
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_government_administration
     }
     has_active_production_method = pm_switch_boards
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_government_administration
   production_method = pm_switch_boards
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_government_administration
     }
     has_active_production_method = pm_hereditary_bureaucrats
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_government_administration
   production_method = pm_hereditary_bureaucrats
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_government_administration
     }
     has_active_production_method = pm_professional_bureaucrats
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_government_administration
   production_method = pm_professional_bureaucrats
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_government_administration
     }
     has_active_production_method = pm_religious_bureaucrats
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_government_administration
   production_method = pm_religious_bureaucrats
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_government_administration
     }
     has_active_production_method = pm_secular_bureaucrats
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_government_administration
   production_method = pm_secular_bureaucrats
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_university
     }
     has_active_production_method = pm_scholastic_education
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_university
   production_method = pm_scholastic_education
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_university
     }
     has_active_production_method = pm_philosophy_department
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_university
   production_method = pm_philosophy_department
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_university
     }
     has_active_production_method = pm_analytical_philosophy_department
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_university
   production_method = pm_analytical_philosophy_department
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_university
     }
     has_active_production_method = pm_religious_academia
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_university
   production_method = pm_religious_academia
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_university
     }
     has_active_production_method = pm_secular_academia
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_university
   production_method = pm_secular_academia
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_logging_camp
     }
     has_active_production_method = pm_simple_forestry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_logging_camp
   production_method = pm_simple_forestry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_logging_camp
     }
     has_active_production_method = pm_saw_mills
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_logging_camp
   production_method = pm_saw_mills
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_logging_camp
     }
     has_active_production_method = pm_electric_saw_mills
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_logging_camp
   production_method = pm_electric_saw_mills
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_logging_camp
     }
     has_active_production_method = pm_no_hardwood
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_logging_camp
   production_method = pm_no_hardwood
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_logging_camp
     }
     has_active_production_method = pm_hardwood
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_logging_camp
   production_method = pm_hardwood
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_logging_camp
     }
     has_active_production_method = pm_no_equipment
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_logging_camp
   production_method = pm_no_equipment
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_logging_camp
     }
     has_active_production_method = pm_steam_donkey_building_logging_camp
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_logging_camp
   production_method = pm_steam_donkey_building_logging_camp
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_logging_camp
     }
     has_active_production_method = pm_chainsaws
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_logging_camp
   production_method = pm_chainsaws
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_logging_camp
     }
     has_active_production_method = pm_road_carts
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_logging_camp
   production_method = pm_road_carts
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_logging_camp
     }
     has_active_production_method = pm_rail_transport_building_logging_camp
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_logging_camp
   production_method = pm_rail_transport_building_logging_camp
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_logging_camp
     }
     has_active_production_method = pm_log_carts
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_logging_camp
   production_method = pm_log_carts
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_logging_camp
     }
     has_active_production_method = pm_merchant_guilds_building_logging_camp
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_logging_camp
   production_method = pm_merchant_guilds_building_logging_camp
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_logging_camp
     }
     has_active_production_method = pm_privately_owned_building_logging_camp
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_logging_camp
   production_method = pm_privately_owned_building_logging_camp
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_logging_camp
     }
     has_active_production_method = pm_publicly_traded_building_logging_camp
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_logging_camp
   production_method = pm_publicly_traded_building_logging_camp
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_logging_camp
     }
     has_active_production_method = pm_government_run_building_logging_camp
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_logging_camp
   production_method = pm_government_run_building_logging_camp
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_logging_camp
     }
     has_active_production_method = pm_worker_cooperative_building_logging_camp
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_logging_camp
   production_method = pm_worker_cooperative_building_logging_camp
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rubber_plantation
     }
     has_active_production_method = pm_road_carts
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rubber_plantation
   production_method = pm_road_carts
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rubber_plantation
     }
     has_active_production_method = pm_steam_rail_transport
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rubber_plantation
   production_method = pm_steam_rail_transport
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rubber_plantation
     }
     has_active_production_method = pm_privately_owned
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rubber_plantation
   production_method = pm_privately_owned
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rubber_plantation
     }
     has_active_production_method = pm_publicly_traded
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rubber_plantation
   production_method = pm_publicly_traded
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rubber_plantation
     }
     has_active_production_method = pm_government_run
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rubber_plantation
   production_method = pm_government_run
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_rubber_plantation
     }
     has_active_production_method = pm_worker_cooperative_plantation
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_rubber_plantation
   production_method = pm_worker_cooperative_plantation
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_fishing_wharf
     }
     has_active_production_method = pm_simple_fishing
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_fishing_wharf
   production_method = pm_simple_fishing
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_fishing_wharf
     }
     has_active_production_method = pm_fishing_trawlers
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_fishing_wharf
   production_method = pm_fishing_trawlers
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_fishing_wharf
     }
     has_active_production_method = pm_steam_trawlers
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_fishing_wharf
   production_method = pm_steam_trawlers
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_fishing_wharf
     }
     has_active_production_method = pm_unrefrigerated
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_fishing_wharf
   production_method = pm_unrefrigerated
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_fishing_wharf
     }
     has_active_production_method = pm_refrigerated_storage_building_fishing_wharf
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_fishing_wharf
   production_method = pm_refrigerated_storage_building_fishing_wharf
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_fishing_wharf
     }
     has_active_production_method = pm_refrigerated_rail_cars_building_fishing_wharf
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_fishing_wharf
   production_method = pm_refrigerated_rail_cars_building_fishing_wharf
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_fishing_wharf
     }
     has_active_production_method = pm_flash_freezing_building_fishing_wharf
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_fishing_wharf
   production_method = pm_flash_freezing_building_fishing_wharf
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_fishing_wharf
     }
     has_active_production_method = pm_merchant_guilds_building_fishing_wharf
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_fishing_wharf
   production_method = pm_merchant_guilds_building_fishing_wharf
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_fishing_wharf
     }
     has_active_production_method = pm_privately_owned_building_fishing_wharf
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_fishing_wharf
   production_method = pm_privately_owned_building_fishing_wharf
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_fishing_wharf
     }
     has_active_production_method = pm_publicly_traded_building_fishing_wharf
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_fishing_wharf
   production_method = pm_publicly_traded_building_fishing_wharf
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_fishing_wharf
     }
     has_active_production_method = pm_government_run_building_fishing_wharf
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_fishing_wharf
   production_method = pm_government_run_building_fishing_wharf
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_fishing_wharf
     }
     has_active_production_method = pm_worker_cooperative_building_fishing_wharf
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_fishing_wharf
   production_method = pm_worker_cooperative_building_fishing_wharf
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_whaling_station
     }
     has_active_production_method = pm_wooden_whaling_ships
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_whaling_station
   production_method = pm_wooden_whaling_ships
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_whaling_station
     }
     has_active_production_method = pm_steam_whaling_ships
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_whaling_station
   production_method = pm_steam_whaling_ships
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_whaling_station
     }
     has_active_production_method = pm_unrefrigerated
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_whaling_station
   production_method = pm_unrefrigerated
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_whaling_station
     }
     has_active_production_method = pm_refrigerated_storage_building_whaling_station
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_whaling_station
   production_method = pm_refrigerated_storage_building_whaling_station
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_whaling_station
     }
     has_active_production_method = pm_refrigerated_rail_cars_building_whaling_station
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_whaling_station
   production_method = pm_refrigerated_rail_cars_building_whaling_station
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_whaling_station
     }
     has_active_production_method = pm_flash_freezing_building_whaling_station
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_whaling_station
   production_method = pm_flash_freezing_building_whaling_station
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_whaling_station
     }
     has_active_production_method = pm_merchant_guilds_building_whaling_station
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_whaling_station
   production_method = pm_merchant_guilds_building_whaling_station
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_whaling_station
     }
     has_active_production_method = pm_privately_owned_building_whaling_station
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_whaling_station
   production_method = pm_privately_owned_building_whaling_station
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_whaling_station
     }
     has_active_production_method = pm_publicly_traded_building_whaling_station
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_whaling_station
   production_method = pm_publicly_traded_building_whaling_station
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_whaling_station
     }
     has_active_production_method = pm_government_run_building_whaling_station
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_whaling_station
   production_method = pm_government_run_building_whaling_station
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_whaling_station
     }
     has_active_production_method = pm_worker_cooperative_building_whaling_station
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_whaling_station
   production_method = pm_worker_cooperative_building_whaling_station
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_oil_rig
     }
     has_active_production_method = pm_steam_derricks
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_oil_rig
   production_method = pm_steam_derricks
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_oil_rig
     }
     has_active_production_method = pm_combustion_derricks
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_oil_rig
   production_method = pm_combustion_derricks
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_oil_rig
     }
     has_active_production_method = pm_road_carts
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_oil_rig
   production_method = pm_road_carts
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_oil_rig
     }
     has_active_production_method = pm_rail_transport_building_oil_rig
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_oil_rig
   production_method = pm_rail_transport_building_oil_rig
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_oil_rig
     }
     has_active_production_method = pm_tanker_cars
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_oil_rig
   production_method = pm_tanker_cars
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_oil_rig
     }
     has_active_production_method = pm_privately_owned_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_oil_rig
   production_method = pm_privately_owned_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_oil_rig
     }
     has_active_production_method = pm_publicly_traded_building_oil_rig
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_oil_rig
   production_method = pm_publicly_traded_building_oil_rig
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_oil_rig
     }
     has_active_production_method = pm_government_run_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_oil_rig
   production_method = pm_government_run_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_oil_rig
     }
     has_active_production_method = pm_worker_cooperative_industry
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_oil_rig
   production_method = pm_worker_cooperative_industry
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_railway
     }
     has_active_production_method = pm_steam_trains
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_railway
   production_method = pm_steam_trains
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_railway
     }
     has_active_production_method = pm_electric_trains
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_railway
   production_method = pm_electric_trains
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_railway
     }
     has_active_production_method = pm_diesel_trains
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_railway
   production_method = pm_diesel_trains
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_railway
     }
     has_active_production_method = pm_no_passenger_trains
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_railway
   production_method = pm_no_passenger_trains
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_railway
     }
     has_active_production_method = pm_wooden_passenger_carriages
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_railway
   production_method = pm_wooden_passenger_carriages
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_railway
     }
     has_active_production_method = pm_steel_passenger_carriages
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_railway
   production_method = pm_steel_passenger_carriages
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_railway
     }
     has_active_production_method = pm_privately_owned_building_railway
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_railway
   production_method = pm_privately_owned_building_railway
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_railway
     }
     has_active_production_method = pm_publicly_traded_building_railway
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_railway
   production_method = pm_publicly_traded_building_railway
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_railway
     }
     has_active_production_method = pm_government_run_building_railway
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_railway
   production_method = pm_government_run_building_railway
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_railway
     }
     has_active_production_method = pm_worker_cooperative_building_railway
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_railway
   production_method = pm_worker_cooperative_building_railway
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_construction_sector
     }
     has_active_production_method = pm_wooden_buildings
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_construction_sector
   production_method = pm_wooden_buildings
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_construction_sector
     }
     has_active_production_method = pm_iron_frame_buildings
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_construction_sector
   production_method = pm_iron_frame_buildings
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_construction_sector
     }
     has_active_production_method = pm_steel_frame_buildings
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_construction_sector
   production_method = pm_steel_frame_buildings
  }
 }
if = {
  limit = {
   root = {
    any_scope_building = {
     filter = {
      state = { has_variable = pm_convert_origin }
      is_building_type = building_construction_sector
     }
     has_active_production_method = pm_arc_welded_buildings
     percent > 0.9
    } 
   }
  }
  activate_production_method = {
   building_type = building_construction_sector
   production_method = pm_arc_welded_buildings
  }
 }



}



